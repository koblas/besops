// Code generated by BobGen sqlite v0.43.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"fmt"
	"io"
	"time"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/sqlite"
	"github.com/stephenafamo/bob/dialect/sqlite/dialect"
	"github.com/stephenafamo/bob/dialect/sqlite/dm"
	"github.com/stephenafamo/bob/dialect/sqlite/sm"
	"github.com/stephenafamo/bob/dialect/sqlite/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
)

// MaintenanceTimeslot is an object representing the database table.
type MaintenanceTimeslot struct {
	ID            string              `db:"id,pk" `
	MaintenanceID string              `db:"maintenance_id" `
	StartDate     time.Time           `db:"start_date" `
	EndDate       null.Val[time.Time] `db:"end_date" `
	GeneratedNext bool                `db:"generated_next" `

	R maintenanceTimeslotR `db:"-" `
}

// MaintenanceTimeslotSlice is an alias for a slice of pointers to MaintenanceTimeslot.
// This should almost always be used instead of []*MaintenanceTimeslot.
type MaintenanceTimeslotSlice []*MaintenanceTimeslot

// MaintenanceTimeslots contains methods to work with the maintenance_timeslot table
var MaintenanceTimeslots = sqlite.NewTablex[*MaintenanceTimeslot, MaintenanceTimeslotSlice, *MaintenanceTimeslotSetter]("", "maintenance_timeslot", buildMaintenanceTimeslotColumns("maintenance_timeslot"))

// MaintenanceTimeslotsQuery is a query on the maintenance_timeslot table
type MaintenanceTimeslotsQuery = *sqlite.ViewQuery[*MaintenanceTimeslot, MaintenanceTimeslotSlice]

// maintenanceTimeslotR is where relationships are stored.
type maintenanceTimeslotR struct {
	Maintenance *Maintenance // fk_maintenance_timeslot_0
}

func buildMaintenanceTimeslotColumns(tableName string) maintenanceTimeslotColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "maintenance_id", "start_date", "end_date", "generated_next",
	)
	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}
	return maintenanceTimeslotColumns{
		ColumnsExpr:   columnsExpr,
		tableAlias:    tableName,
		ID:            sqlite.Quote(tableName, "id"),
		MaintenanceID: sqlite.Quote(tableName, "maintenance_id"),
		StartDate:     sqlite.Quote(tableName, "start_date"),
		EndDate:       sqlite.Quote(tableName, "end_date"),
		GeneratedNext: sqlite.Quote(tableName, "generated_next"),
	}
}

type maintenanceTimeslotColumns struct {
	expr.ColumnsExpr
	tableAlias    string
	ID            sqlite.Expression
	MaintenanceID sqlite.Expression
	StartDate     sqlite.Expression
	EndDate       sqlite.Expression
	GeneratedNext sqlite.Expression
}

func (c maintenanceTimeslotColumns) Alias() string {
	return c.tableAlias
}

func (maintenanceTimeslotColumns) AliasedAs(tableName string) maintenanceTimeslotColumns {
	return buildMaintenanceTimeslotColumns(tableName)
}

func (c maintenanceTimeslotColumns) Unqualified() maintenanceTimeslotColumns {
	return buildMaintenanceTimeslotColumns("")
}

// MaintenanceTimeslotSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type MaintenanceTimeslotSetter struct {
	ID            omit.Val[string]        `db:"id,pk" `
	MaintenanceID omit.Val[string]        `db:"maintenance_id" `
	StartDate     omit.Val[time.Time]     `db:"start_date" `
	EndDate       omitnull.Val[time.Time] `db:"end_date" `
	GeneratedNext omit.Val[bool]          `db:"generated_next" `
}

func (s MaintenanceTimeslotSetter) SetColumns() []string {
	vals := make([]string, 0, 5)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.MaintenanceID.IsValue() {
		vals = append(vals, "maintenance_id")
	}
	if s.StartDate.IsValue() {
		vals = append(vals, "start_date")
	}
	if !s.EndDate.IsUnset() {
		vals = append(vals, "end_date")
	}
	if s.GeneratedNext.IsValue() {
		vals = append(vals, "generated_next")
	}
	return vals
}

func (s MaintenanceTimeslotSetter) Overwrite(t *MaintenanceTimeslot) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.MaintenanceID.IsValue() {
		t.MaintenanceID = s.MaintenanceID.MustGet()
	}
	if s.StartDate.IsValue() {
		t.StartDate = s.StartDate.MustGet()
	}
	if !s.EndDate.IsUnset() {
		t.EndDate = s.EndDate.MustGetNull()
	}
	if s.GeneratedNext.IsValue() {
		t.GeneratedNext = s.GeneratedNext.MustGet()
	}
}

func (s *MaintenanceTimeslotSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return MaintenanceTimeslots.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	if len(q.TableRef.Columns) == 0 {
		q.TableRef.Columns = s.SetColumns()
		if len(q.TableRef.Columns) == 0 {
			q.TableRef.Columns = []string{"id"}
		}

	}

	q.AppendValues(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		vals := make([]bob.Expression, 0, 5)
		if s.ID.IsValue() {
			vals = append(vals, sqlite.Arg(s.ID.MustGet()))
		}

		if s.MaintenanceID.IsValue() {
			vals = append(vals, sqlite.Arg(s.MaintenanceID.MustGet()))
		}

		if s.StartDate.IsValue() {
			vals = append(vals, sqlite.Arg(s.StartDate.MustGet()))
		}

		if !s.EndDate.IsUnset() {
			vals = append(vals, sqlite.Arg(s.EndDate.MustGetNull()))
		}

		if s.GeneratedNext.IsValue() {
			vals = append(vals, sqlite.Arg(s.GeneratedNext.MustGet()))
		}

		if len(vals) == 0 {
			vals = append(vals, sqlite.Arg(nil))
		}

		return bob.ExpressSlice(ctx, w, d, start, vals, "", ", ", "")
	}))
}

func (s MaintenanceTimeslotSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s MaintenanceTimeslotSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 5)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "id")...),
			sqlite.Arg(s.ID),
		}})
	}

	if s.MaintenanceID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "maintenance_id")...),
			sqlite.Arg(s.MaintenanceID),
		}})
	}

	if s.StartDate.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "start_date")...),
			sqlite.Arg(s.StartDate),
		}})
	}

	if !s.EndDate.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "end_date")...),
			sqlite.Arg(s.EndDate),
		}})
	}

	if s.GeneratedNext.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "generated_next")...),
			sqlite.Arg(s.GeneratedNext),
		}})
	}

	return exprs
}

// FindMaintenanceTimeslot retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindMaintenanceTimeslot(ctx context.Context, exec bob.Executor, IDPK string, cols ...string) (*MaintenanceTimeslot, error) {
	if len(cols) == 0 {
		return MaintenanceTimeslots.Query(
			sm.Where(MaintenanceTimeslots.Columns.ID.EQ(sqlite.Arg(IDPK))),
		).One(ctx, exec)
	}

	return MaintenanceTimeslots.Query(
		sm.Where(MaintenanceTimeslots.Columns.ID.EQ(sqlite.Arg(IDPK))),
		sm.Columns(MaintenanceTimeslots.Columns.Only(cols...)),
	).One(ctx, exec)
}

// MaintenanceTimeslotExists checks the presence of a single record by primary key
func MaintenanceTimeslotExists(ctx context.Context, exec bob.Executor, IDPK string) (bool, error) {
	return MaintenanceTimeslots.Query(
		sm.Where(MaintenanceTimeslots.Columns.ID.EQ(sqlite.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after MaintenanceTimeslot is retrieved from the database
func (o *MaintenanceTimeslot) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = MaintenanceTimeslots.AfterSelectHooks.RunHooks(ctx, exec, MaintenanceTimeslotSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = MaintenanceTimeslots.AfterInsertHooks.RunHooks(ctx, exec, MaintenanceTimeslotSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = MaintenanceTimeslots.AfterUpdateHooks.RunHooks(ctx, exec, MaintenanceTimeslotSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = MaintenanceTimeslots.AfterDeleteHooks.RunHooks(ctx, exec, MaintenanceTimeslotSlice{o})

	}

	return err
}

// primaryKeyVals returns the primary key values of the MaintenanceTimeslot
func (o *MaintenanceTimeslot) primaryKeyVals() bob.Expression {
	return sqlite.Arg(o.ID)
}

func (o *MaintenanceTimeslot) pkEQ() dialect.Expression {
	return sqlite.Quote("maintenance_timeslot", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the MaintenanceTimeslot
func (o *MaintenanceTimeslot) Update(ctx context.Context, exec bob.Executor, s *MaintenanceTimeslotSetter) error {
	v, err := MaintenanceTimeslots.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single MaintenanceTimeslot record with an executor
func (o *MaintenanceTimeslot) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := MaintenanceTimeslots.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the MaintenanceTimeslot using the executor
func (o *MaintenanceTimeslot) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := MaintenanceTimeslots.Query(
		sm.Where(MaintenanceTimeslots.Columns.ID.EQ(sqlite.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after MaintenanceTimeslotSlice is retrieved from the database
func (o MaintenanceTimeslotSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = MaintenanceTimeslots.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = MaintenanceTimeslots.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = MaintenanceTimeslots.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = MaintenanceTimeslots.AfterDeleteHooks.RunHooks(ctx, exec, o)

	}

	return err
}

func (o MaintenanceTimeslotSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return sqlite.Raw("NULL")
	}

	return sqlite.Quote("maintenance_timeslot", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o MaintenanceTimeslotSlice) copyMatchingRows(from ...*MaintenanceTimeslot) {
	for i, old := range o {
		for _, new := range from {
			if new.ID != old.ID {
				continue
			}
			new.R = old.R
			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o MaintenanceTimeslotSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return MaintenanceTimeslots.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *MaintenanceTimeslot:
				o.copyMatchingRows(retrieved)
			case []*MaintenanceTimeslot:
				o.copyMatchingRows(retrieved...)
			case MaintenanceTimeslotSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a MaintenanceTimeslot or a slice of MaintenanceTimeslot
				// then run the AfterUpdateHooks on the slice
				_, err = MaintenanceTimeslots.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o MaintenanceTimeslotSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return MaintenanceTimeslots.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *MaintenanceTimeslot:
				o.copyMatchingRows(retrieved)
			case []*MaintenanceTimeslot:
				o.copyMatchingRows(retrieved...)
			case MaintenanceTimeslotSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a MaintenanceTimeslot or a slice of MaintenanceTimeslot
				// then run the AfterDeleteHooks on the slice
				_, err = MaintenanceTimeslots.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

func (o MaintenanceTimeslotSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals MaintenanceTimeslotSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := MaintenanceTimeslots.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o MaintenanceTimeslotSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := MaintenanceTimeslots.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o MaintenanceTimeslotSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := MaintenanceTimeslots.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// Maintenance starts a query for related objects on maintenance
func (o *MaintenanceTimeslot) Maintenance(mods ...bob.Mod[*dialect.SelectQuery]) MaintenancesQuery {
	return Maintenances.Query(append(mods,
		sm.Where(Maintenances.Columns.ID.EQ(sqlite.Arg(o.MaintenanceID))),
	)...)
}

func (os MaintenanceTimeslotSlice) Maintenance(mods ...bob.Mod[*dialect.SelectQuery]) MaintenancesQuery {
	PKArgSlice := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgSlice[i] = sqlite.ArgGroup(o.MaintenanceID)
	}
	PKArgExpr := sqlite.Group(PKArgSlice...)

	return Maintenances.Query(append(mods,
		sm.Where(sqlite.Group(Maintenances.Columns.ID).OP("IN", PKArgExpr)),
	)...)
}

func attachMaintenanceTimeslotMaintenance0(ctx context.Context, exec bob.Executor, count int, maintenanceTimeslot0 *MaintenanceTimeslot, maintenance1 *Maintenance) (*MaintenanceTimeslot, error) {
	setter := &MaintenanceTimeslotSetter{
		MaintenanceID: omit.From(maintenance1.ID),
	}

	err := maintenanceTimeslot0.Update(ctx, exec, setter)
	if err != nil {
		return nil, fmt.Errorf("attachMaintenanceTimeslotMaintenance0: %w", err)
	}

	return maintenanceTimeslot0, nil
}

func (maintenanceTimeslot0 *MaintenanceTimeslot) InsertMaintenance(ctx context.Context, exec bob.Executor, related *MaintenanceSetter) error {
	var err error

	maintenance1, err := Maintenances.Insert(related).One(ctx, exec)
	if err != nil {
		return fmt.Errorf("inserting related objects: %w", err)
	}

	_, err = attachMaintenanceTimeslotMaintenance0(ctx, exec, 1, maintenanceTimeslot0, maintenance1)
	if err != nil {
		return err
	}

	maintenanceTimeslot0.R.Maintenance = maintenance1

	maintenance1.R.MaintenanceTimeslots = append(maintenance1.R.MaintenanceTimeslots, maintenanceTimeslot0)

	return nil
}

func (maintenanceTimeslot0 *MaintenanceTimeslot) AttachMaintenance(ctx context.Context, exec bob.Executor, maintenance1 *Maintenance) error {
	var err error

	_, err = attachMaintenanceTimeslotMaintenance0(ctx, exec, 1, maintenanceTimeslot0, maintenance1)
	if err != nil {
		return err
	}

	maintenanceTimeslot0.R.Maintenance = maintenance1

	maintenance1.R.MaintenanceTimeslots = append(maintenance1.R.MaintenanceTimeslots, maintenanceTimeslot0)

	return nil
}

type maintenanceTimeslotWhere[Q sqlite.Filterable] struct {
	ID            sqlite.WhereMod[Q, string]
	MaintenanceID sqlite.WhereMod[Q, string]
	StartDate     sqlite.WhereMod[Q, time.Time]
	EndDate       sqlite.WhereNullMod[Q, time.Time]
	GeneratedNext sqlite.WhereMod[Q, bool]
}

func (maintenanceTimeslotWhere[Q]) AliasedAs(alias string) maintenanceTimeslotWhere[Q] {
	return buildMaintenanceTimeslotWhere[Q](buildMaintenanceTimeslotColumns(alias))
}

func buildMaintenanceTimeslotWhere[Q sqlite.Filterable](cols maintenanceTimeslotColumns) maintenanceTimeslotWhere[Q] {
	return maintenanceTimeslotWhere[Q]{
		ID:            sqlite.Where[Q, string](cols.ID),
		MaintenanceID: sqlite.Where[Q, string](cols.MaintenanceID),
		StartDate:     sqlite.Where[Q, time.Time](cols.StartDate),
		EndDate:       sqlite.WhereNull[Q, time.Time](cols.EndDate),
		GeneratedNext: sqlite.Where[Q, bool](cols.GeneratedNext),
	}
}

func (o *MaintenanceTimeslot) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Maintenance":
		rel, ok := retrieved.(*Maintenance)
		if !ok {
			return fmt.Errorf("maintenanceTimeslot cannot load %T as %q", retrieved, name)
		}

		o.R.Maintenance = rel

		if rel != nil {
			rel.R.MaintenanceTimeslots = MaintenanceTimeslotSlice{o}
		}
		return nil
	default:
		return fmt.Errorf("maintenanceTimeslot has no relationship %q", name)
	}
}

type maintenanceTimeslotPreloader struct {
	Maintenance func(...sqlite.PreloadOption) sqlite.Preloader
}

func buildMaintenanceTimeslotPreloader() maintenanceTimeslotPreloader {
	return maintenanceTimeslotPreloader{
		Maintenance: func(opts ...sqlite.PreloadOption) sqlite.Preloader {
			return sqlite.Preload[*Maintenance, MaintenanceSlice](sqlite.PreloadRel{
				Name: "Maintenance",
				Sides: []sqlite.PreloadSide{
					{
						From:        MaintenanceTimeslots,
						To:          Maintenances,
						FromColumns: []string{"maintenance_id"},
						ToColumns:   []string{"id"},
					},
				},
			}, Maintenances.Columns.Names(), opts...)
		},
	}
}

type maintenanceTimeslotThenLoader[Q orm.Loadable] struct {
	Maintenance func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildMaintenanceTimeslotThenLoader[Q orm.Loadable]() maintenanceTimeslotThenLoader[Q] {
	type MaintenanceLoadInterface interface {
		LoadMaintenance(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return maintenanceTimeslotThenLoader[Q]{
		Maintenance: thenLoadBuilder[Q](
			"Maintenance",
			func(ctx context.Context, exec bob.Executor, retrieved MaintenanceLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadMaintenance(ctx, exec, mods...)
			},
		),
	}
}

// LoadMaintenance loads the maintenanceTimeslot's Maintenance into the .R struct
func (o *MaintenanceTimeslot) LoadMaintenance(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Maintenance = nil

	related, err := o.Maintenance(mods...).One(ctx, exec)
	if err != nil {
		return err
	}

	related.R.MaintenanceTimeslots = MaintenanceTimeslotSlice{o}

	o.R.Maintenance = related
	return nil
}

// LoadMaintenance loads the maintenanceTimeslot's Maintenance into the .R struct
func (os MaintenanceTimeslotSlice) LoadMaintenance(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	maintenances, err := os.Maintenance(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range maintenances {

			if !(o.MaintenanceID == rel.ID) {
				continue
			}

			rel.R.MaintenanceTimeslots = append(rel.R.MaintenanceTimeslots, o)

			o.R.Maintenance = rel
			break
		}
	}

	return nil
}

type maintenanceTimeslotJoins[Q dialect.Joinable] struct {
	typ         string
	Maintenance modAs[Q, maintenanceColumns]
}

func (j maintenanceTimeslotJoins[Q]) aliasedAs(alias string) maintenanceTimeslotJoins[Q] {
	return buildMaintenanceTimeslotJoins[Q](buildMaintenanceTimeslotColumns(alias), j.typ)
}

func buildMaintenanceTimeslotJoins[Q dialect.Joinable](cols maintenanceTimeslotColumns, typ string) maintenanceTimeslotJoins[Q] {
	return maintenanceTimeslotJoins[Q]{
		typ: typ,
		Maintenance: modAs[Q, maintenanceColumns]{
			c: Maintenances.Columns,
			f: func(to maintenanceColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Maintenances.Name().As(to.Alias())).On(
						to.ID.EQ(cols.MaintenanceID),
					))
				}

				return mods
			},
		},
	}
}
