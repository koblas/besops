// Code generated by BobGen sqlite v0.43.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"
	"time"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/jaswdr/faker/v2"
	models "github.com/koblas/besops/models"
	"github.com/stephenafamo/bob"
)

type DomainExpiryMod interface {
	Apply(context.Context, *DomainExpiryTemplate)
}

type DomainExpiryModFunc func(context.Context, *DomainExpiryTemplate)

func (f DomainExpiryModFunc) Apply(ctx context.Context, n *DomainExpiryTemplate) {
	f(ctx, n)
}

type DomainExpiryModSlice []DomainExpiryMod

func (mods DomainExpiryModSlice) Apply(ctx context.Context, n *DomainExpiryTemplate) {
	for _, f := range mods {
		f.Apply(ctx, n)
	}
}

// DomainExpiryTemplate is an object representing the database table.
// all columns are optional and should be set by mods
type DomainExpiryTemplate struct {
	ID                         func() string
	LastCheck                  func() null.Val[time.Time]
	Domain                     func() string
	Expiry                     func() null.Val[time.Time]
	LastExpiryNotificationSent func() null.Val[time.Time]

	f *Factory

	alreadyPersisted bool
}

// Apply mods to the DomainExpiryTemplate
func (o *DomainExpiryTemplate) Apply(ctx context.Context, mods ...DomainExpiryMod) {
	for _, mod := range mods {
		mod.Apply(ctx, o)
	}
}

// setModelRels creates and sets the relationships on *models.DomainExpiry
// according to the relationships in the template. Nothing is inserted into the db
func (t DomainExpiryTemplate) setModelRels(o *models.DomainExpiry) {}

// BuildSetter returns an *models.DomainExpirySetter
// this does nothing with the relationship templates
func (o DomainExpiryTemplate) BuildSetter() *models.DomainExpirySetter {
	m := &models.DomainExpirySetter{}

	if o.ID != nil {
		val := o.ID()
		m.ID = omit.From(val)
	}
	if o.LastCheck != nil {
		val := o.LastCheck()
		m.LastCheck = omitnull.FromNull(val)
	}
	if o.Domain != nil {
		val := o.Domain()
		m.Domain = omit.From(val)
	}
	if o.Expiry != nil {
		val := o.Expiry()
		m.Expiry = omitnull.FromNull(val)
	}
	if o.LastExpiryNotificationSent != nil {
		val := o.LastExpiryNotificationSent()
		m.LastExpiryNotificationSent = omitnull.FromNull(val)
	}

	return m
}

// BuildManySetter returns an []*models.DomainExpirySetter
// this does nothing with the relationship templates
func (o DomainExpiryTemplate) BuildManySetter(number int) []*models.DomainExpirySetter {
	m := make([]*models.DomainExpirySetter, number)

	for i := range m {
		m[i] = o.BuildSetter()
	}

	return m
}

// Build returns an *models.DomainExpiry
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use DomainExpiryTemplate.Create
func (o DomainExpiryTemplate) Build() *models.DomainExpiry {
	m := &models.DomainExpiry{}

	if o.ID != nil {
		m.ID = o.ID()
	}
	if o.LastCheck != nil {
		m.LastCheck = o.LastCheck()
	}
	if o.Domain != nil {
		m.Domain = o.Domain()
	}
	if o.Expiry != nil {
		m.Expiry = o.Expiry()
	}
	if o.LastExpiryNotificationSent != nil {
		m.LastExpiryNotificationSent = o.LastExpiryNotificationSent()
	}

	o.setModelRels(m)

	return m
}

// BuildMany returns an models.DomainExpirySlice
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use DomainExpiryTemplate.CreateMany
func (o DomainExpiryTemplate) BuildMany(number int) models.DomainExpirySlice {
	m := make(models.DomainExpirySlice, number)

	for i := range m {
		m[i] = o.Build()
	}

	return m
}

func ensureCreatableDomainExpiry(m *models.DomainExpirySetter) {
	if !(m.ID.IsValue()) {
		val := random_string(nil)
		m.ID = omit.From(val)
	}
	if !(m.Domain.IsValue()) {
		val := random_string(nil)
		m.Domain = omit.From(val)
	}
}

// insertOptRels creates and inserts any optional the relationships on *models.DomainExpiry
// according to the relationships in the template.
// any required relationship should have already exist on the model
func (o *DomainExpiryTemplate) insertOptRels(ctx context.Context, exec bob.Executor, m *models.DomainExpiry) error {
	var err error

	return err
}

// Create builds a domainExpiry and inserts it into the database
// Relations objects are also inserted and placed in the .R field
func (o *DomainExpiryTemplate) Create(ctx context.Context, exec bob.Executor) (*models.DomainExpiry, error) {
	var err error
	opt := o.BuildSetter()
	ensureCreatableDomainExpiry(opt)

	m, err := models.DomainExpiries.Insert(opt).One(ctx, exec)
	if err != nil {
		return nil, err
	}

	if err := o.insertOptRels(ctx, exec, m); err != nil {
		return nil, err
	}
	return m, err
}

// MustCreate builds a domainExpiry and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o *DomainExpiryTemplate) MustCreate(ctx context.Context, exec bob.Executor) *models.DomainExpiry {
	m, err := o.Create(ctx, exec)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateOrFail builds a domainExpiry and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o *DomainExpiryTemplate) CreateOrFail(ctx context.Context, tb testing.TB, exec bob.Executor) *models.DomainExpiry {
	tb.Helper()
	m, err := o.Create(ctx, exec)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// CreateMany builds multiple domainExpiries and inserts them into the database
// Relations objects are also inserted and placed in the .R field
func (o DomainExpiryTemplate) CreateMany(ctx context.Context, exec bob.Executor, number int) (models.DomainExpirySlice, error) {
	var err error
	m := make(models.DomainExpirySlice, number)

	for i := range m {
		m[i], err = o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	return m, nil
}

// MustCreateMany builds multiple domainExpiries and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o DomainExpiryTemplate) MustCreateMany(ctx context.Context, exec bob.Executor, number int) models.DomainExpirySlice {
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateManyOrFail builds multiple domainExpiries and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o DomainExpiryTemplate) CreateManyOrFail(ctx context.Context, tb testing.TB, exec bob.Executor, number int) models.DomainExpirySlice {
	tb.Helper()
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// DomainExpiry has methods that act as mods for the DomainExpiryTemplate
var DomainExpiryMods domainExpiryMods

type domainExpiryMods struct{}

func (m domainExpiryMods) RandomizeAllColumns(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModSlice{
		DomainExpiryMods.RandomID(f),
		DomainExpiryMods.RandomLastCheck(f),
		DomainExpiryMods.RandomDomain(f),
		DomainExpiryMods.RandomExpiry(f),
		DomainExpiryMods.RandomLastExpiryNotificationSent(f),
	}
}

// Set the model columns to this value
func (m domainExpiryMods) ID(val string) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.ID = func() string { return val }
	})
}

// Set the Column from the function
func (m domainExpiryMods) IDFunc(f func() string) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.ID = f
	})
}

// Clear any values for the column
func (m domainExpiryMods) UnsetID() DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.ID = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m domainExpiryMods) RandomID(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.ID = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m domainExpiryMods) LastCheck(val null.Val[time.Time]) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastCheck = func() null.Val[time.Time] { return val }
	})
}

// Set the Column from the function
func (m domainExpiryMods) LastCheckFunc(f func() null.Val[time.Time]) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastCheck = f
	})
}

// Clear any values for the column
func (m domainExpiryMods) UnsetLastCheck() DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastCheck = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is sometimes null
func (m domainExpiryMods) RandomLastCheck(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastCheck = func() null.Val[time.Time] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_time_Time(f)
			return null.From(val)
		}
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is never null
func (m domainExpiryMods) RandomLastCheckNotNull(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastCheck = func() null.Val[time.Time] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_time_Time(f)
			return null.From(val)
		}
	})
}

// Set the model columns to this value
func (m domainExpiryMods) Domain(val string) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Domain = func() string { return val }
	})
}

// Set the Column from the function
func (m domainExpiryMods) DomainFunc(f func() string) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Domain = f
	})
}

// Clear any values for the column
func (m domainExpiryMods) UnsetDomain() DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Domain = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m domainExpiryMods) RandomDomain(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Domain = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m domainExpiryMods) Expiry(val null.Val[time.Time]) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Expiry = func() null.Val[time.Time] { return val }
	})
}

// Set the Column from the function
func (m domainExpiryMods) ExpiryFunc(f func() null.Val[time.Time]) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Expiry = f
	})
}

// Clear any values for the column
func (m domainExpiryMods) UnsetExpiry() DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Expiry = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is sometimes null
func (m domainExpiryMods) RandomExpiry(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Expiry = func() null.Val[time.Time] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_time_Time(f)
			return null.From(val)
		}
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is never null
func (m domainExpiryMods) RandomExpiryNotNull(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.Expiry = func() null.Val[time.Time] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_time_Time(f)
			return null.From(val)
		}
	})
}

// Set the model columns to this value
func (m domainExpiryMods) LastExpiryNotificationSent(val null.Val[time.Time]) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastExpiryNotificationSent = func() null.Val[time.Time] { return val }
	})
}

// Set the Column from the function
func (m domainExpiryMods) LastExpiryNotificationSentFunc(f func() null.Val[time.Time]) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastExpiryNotificationSent = f
	})
}

// Clear any values for the column
func (m domainExpiryMods) UnsetLastExpiryNotificationSent() DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastExpiryNotificationSent = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is sometimes null
func (m domainExpiryMods) RandomLastExpiryNotificationSent(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastExpiryNotificationSent = func() null.Val[time.Time] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_time_Time(f)
			return null.From(val)
		}
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is never null
func (m domainExpiryMods) RandomLastExpiryNotificationSentNotNull(f *faker.Faker) DomainExpiryMod {
	return DomainExpiryModFunc(func(_ context.Context, o *DomainExpiryTemplate) {
		o.LastExpiryNotificationSent = func() null.Val[time.Time] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_time_Time(f)
			return null.From(val)
		}
	})
}

func (m domainExpiryMods) WithParentsCascading() DomainExpiryMod {
	return DomainExpiryModFunc(func(ctx context.Context, o *DomainExpiryTemplate) {
		if isDone, _ := domainExpiryWithParentsCascadingCtx.Value(ctx); isDone {
			return
		}
		ctx = domainExpiryWithParentsCascadingCtx.WithValue(ctx, true)
	})
}
